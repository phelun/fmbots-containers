pipeline {
    agent any

    stages {
        stage('FmDecDemo') {
            steps {
                echo 'Hello, World!'
            }
        }
    }

    post {
        success {
            echo 'This will run if the pipeline succeeds.'
        }
        failure {
            echo 'This will run if the pipeline fails.'
        }
    }
}
